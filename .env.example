# =========================
# Stockelper Portfolio API
# =========================

# ---------- Server ----------
HOST=0.0.0.0
PORT=21008
DEBUG=false

# ---------- Database (필수) ----------
# stockelper_web DB(또는 schema=stockelper_web)가 있는 Postgres로 연결되어야 합니다.
# 이 서비스는 user_id 기반으로 아래 테이블을 참조합니다.
# - stockelper_web.user: kis_app_key, kis_app_secret, kis_access_token, account_no
# - stockelper_web.survey: answer(JSON)
DATABASE_URL=postgresql://user:password@host:5432/stockelper_web
ASYNC_DATABASE_URL=

# (선택) schema 명이 stockelper_web가 아니라면 지정
STOCKELPER_WEB_SCHEMA=public

# (선택) KSIC 산업분류 DB
ASYNC_DATABASE_URL_KSIC=

# ---------- External APIs ----------
# DART(금감원) - 기업 정보/재무에 사용
OPEN_DART_API_KEY=

# OpenRouter - WebSearch/ViewGenerator/SellDecisionMaker 등 LLM 호출에 사용
OPENROUTER_API_KEY=

# (선택) KIS 호출 Rate Limit (초당 최대 요청 수)
KIS_MAX_REQUESTS_PER_SECOND=20

# (추천/분석 전용) KIS 호출 RPS 제한 (모의투자는 특히 낮게 권장: 1~2)
KIS_ANALYSIS_MAX_REQUESTS_PER_SECOND=1
